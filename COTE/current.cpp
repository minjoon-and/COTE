#include "utils/fileio.h"  // 제출 시 삭제

#include <iostream>
#include <vector>

using namespace std;


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    
    int test;
    cin >> test;
    cout << test;

    return 0;
}